--- a/netlify/functions/nsw-api-endpoints.js
+++ b/netlify/functions/nsw-api-endpoints.js
@@ -1,3 +1,12 @@
 // Ensure these functions are exported for other functions to import.
+export {
+  getNSWTextTypeAnalysis,
+  getNSWVocabularySophistication,
+  getNSWProgressTracking,
+  getNSWCoachingTips
+};
+
+// If your file defines them but doesn't export, add the above block.
+// If they are default-exported or named differently, adapt accordingly.
